<ul>
  <% phone_numbers.each do |phone| %>
    <li><%= phone.number %> <%= link_to('edit', edit_phone_number_path(phone)) %> | <%= link_to 'Destroy', phone, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>
